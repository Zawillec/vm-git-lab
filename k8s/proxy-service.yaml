apiVersion: v1
kind: Service
metadata:
  name: vm-git-lab-proxy
spec:
  type: NodePort
  selector:
    app: vm-git-lab-proxy
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
